#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/vtak/Dev/pelilauta-store/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/vtak/Dev/pelilauta-store/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../jsdoc@4.0.2/node_modules/jsdoc/jsdoc.js" "$@"
else
  exec node  "$basedir/../../../../../jsdoc@4.0.2/node_modules/jsdoc/jsdoc.js" "$@"
fi
